<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <%= link_to admin_root_path, class: 'navbar-brand' do %>
            Tekton News Admin
        <% end %>
    </div>
    <% if admin_user_signed_in? %>
        <!-- Top Menu Items -->
        <ul class="nav navbar-right top-nav">
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user"></i> Julio Castillo <b class="caret"></b></a>
                <ul class="dropdown-menu">
                    <li>
                        <%= link_to edit_admin_admin_user_path(current_admin_user) do %>
                            </i> Profile</a>
                        <% end %>
                    </li>
                    <li class="divider"></li>
                    <li>
                        <%= link_to destroy_admin_user_session_path, method: :delete do %>
                            <i class="fa fa-fw fa-power-off"></i> Log Out
                        <% end %>
                    </li>
                </ul>
            </li>
        </ul>
        <!-- Sidebar Menu Items - These collapse to the responsive navigation menu on small screens -->
        <div class="collapse navbar-collapse navbar-ex1-collapse">
            <ul class="nav navbar-nav side-nav">
                <li>
                    <%= link_to admin_admin_users_path do %>
                        <i class="fa fa-fw fa-users"></i> Admin Users
                    <% end %>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    <% end %>
</nav>